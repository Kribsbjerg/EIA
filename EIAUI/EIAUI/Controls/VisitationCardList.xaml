<UserControl x:Class="EIAUI.VisitationCardList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EIAUI"
             mc:Ignorable="d" 
             
             d:DesignHeight="450" d:DesignWidth="620">
    <Grid DataContext="{x:Static local:VisitationCardListDesignModel.Instance}" Background="{StaticResource BackgroundColorBrush}">
        <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="0, 0, 0, 0">
            <ItemsControl ItemsSource="{Binding VisitationCards}" Margin="0 12 0 0">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <local:VisitationCardAlt Padding="0 5" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
        </Grid>
</UserControl>
